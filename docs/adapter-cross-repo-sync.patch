diff --git a/docs/plugin-admission-and-development.md b/docs/plugin-admission-and-development.md
--- a/docs/plugin-admission-and-development.md
+++ b/docs/plugin-admission-and-development.md
@@ -129,7 +129,7 @@
 - `dsh-ecosystem-spec/` — 通过 submodule 固定 admission profile 的只读 registry / schema /
   conformance fixtures；`npm run verify:plugin-spec` 检查 profile hash 与
   fixtures 漂移。
-- `src/plugin-spec/` — 校验/协商纯库：官方 parser 负责 manifest 形状，
+- `src/adapter/standard/` — 校验/协商纯库：官方 parser 负责 manifest 形状，
   `validatePlugin`/`validateHost` 负责 profile 语义，五态 `negotiate` 使用
   同一 `ProtocolCatalog`。
diff --git a/adapters/dsh-tui-v0.15.md b/adapters/dsh-tui-v0.15.md
--- a/adapters/dsh-tui-v0.15.md
+++ b/adapters/dsh-tui-v0.15.md
@@ -3,7 +3,7 @@
 **Status:** Current
 **Spec version:** community-v0.15 + tui-admission/0.15（`registry/registry-0.15.json`）
 **Host:** dsh-TUI 0.9.x（`@deepseek-harness-tui/dsh-tui`）/ Cordis 4.x profile；上游 `@deepseek-ai/*` 主校验线 `0.1.2-alpha.1`（兼容 `0.1.0-rc.6/7/8`、`0.1.1-rc.1/2`）
-**仓库:** https://github.com/ccch1mneyyy/dsh-TUI（`src/dsh-adapter/`、`src/plugin-spec/`）
+**仓库:** https://github.com/ccch1mneyyy/dsh-TUI（`src/dsh-adapter/`、`src/adapter/standard/`）
 
 ## 定位
 
本 Note 是 **dsh-tui 本体作为宿主**的 Adapter Note：它把公共 contract 坐标逐一映射到
 dsh-tui 维护的 adapter 中介面（Cordis service id / 宿主 API），记录已知偏差与证据。
